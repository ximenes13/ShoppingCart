# 🛒 Shopping Cart App with Python + Tkinter

This project is a desktop application built using Python and Tkinter. It allows users to manage a shopping cart by adding items, specifying prices and quantities, removing items, clearing the cart, and tracking a history of actions. The app also supports light and dark themes for a better user experience.

---

## 🚀 Features
📥 Input fields for Item name, Price, and Quantity <br>
✅ Add items to the cart and update quantities automatically <br>
❌ Remove selected items from the cart <br>
🧹 Clear the entire cart with one click <br>
📜 Action history with timestamps to track all cart changes <br>
💰 Automatic total price calculation <br>
🌙☀️ Switch between light and dark themes <br>
❌ Error handling for invalid inputs (non-numeric price/quantity) <br>
🪟 Clean, responsive layout using Tkinter widgets <br>
💡 Pure Python implementation compatible with Python 3.x (no external libraries required) <br>

---

## 🖥️ Technologies Used

- Python 3.x
- Tkinter for the graphical user interface
- Built-in datetime module for action timestamps
- Compatible with PyCharm, VSCode, or any Python IDE

---

## 📂 Project Structure

- **main.py: Core script that handles layout and business logic
    - 🖼️ Creates GUI with Tkinter widgets: Label, Entry, Button, Checkbutton, Frame, Text <br>
    - 🧠 Manages cart operations: add, remove, clear, calculate total <br>
    - 📜 Tracks action history with timestamps <br>
    - 🌙 Supports light and dark themes <br>
    - ❌ Handles invalid input errors gracefully <br>
    - 🧩 Modular and easy-to-read code for future extensions <br>
---

## 🛠️ Setup

### Step 1: Clone the Repository

To get started, clone this repository to your local machine using the following command:

`git clone https://github.com/your-username/ShoppingCartApp.git`

### Step 2: Dependencies

Make sure you have Python 3.x installed. You can check your version with:

`python3 --version`

### Step 3: Run the project

Once you've installed the dependencies, you can run the main Python script to generate and interact with the calculator app.

`python3 main.py`

--- 

## 🤝 Contributing

Contributions are welcome! If you'd like to improve the project, feel free to:

1. Fork the repository.
2. Create a new branch (`git checkout -b feature-name`).
3. Make your changes and commit them (`git commit -m 'Add new feature'`).
4. Push to your branch (`git push origin feature-name`).
5. Submit a pull request.

If you find bugs or have feature requests, please [open an issue](https://github.com/ximenes13/ShoppingCartApp/issues).

